# EISLAW Agent Orchestrator Dependencies
# Per PRD_AGENT_FRAMEWORK_LANGSMITH.md Section 7.8
#
# This file contains dependencies for the orchestrator service
# which manages agent workflows using Microsoft Agent Framework + Langfuse
# CEO Decision (2025-12-09): Use Langfuse (self-hosted, free) instead of LangSmith

# Microsoft Agent Framework (beta release)
# Note: This is a pre-release version - verify API compatibility before production
agent-framework>=1.0.0b251120

# Langfuse for observability, tracing, and metrics (self-hosted)
# Replaces LangSmith per CEO decision - $0/month, unlimited traces
langfuse>=2.0.0

# LangChain Anthropic integration for Claude API calls
langchain-anthropic>=0.3.0

# FastAPI for the health check and trigger endpoints
fastapi>=0.104.0

# Uvicorn ASGI server
uvicorn>=0.24.0

# HTTP client for API calls
httpx>=0.25.0
