version: "3.9"

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.api
    env_file:
      - .env.local
    environment:
      - PORT=${API_PORT:-8799}
      - DEV_CORS_ORIGINS=http://20.217.86.4:5173,http://20.217.86.4:8080,http://localhost:5173,http://localhost:8080
      - MEILI_URL=${MEILI_URL:-http://meili:7700}
    command: uvicorn backend.main:app --host 0.0.0.0 --port 8799 --reload
    ports:
      - "${API_PORT:-8799}:8799"
    volumes:
      - ./backend:/app/backend
      - ./scoring_service:/app/scoring_service
      - ./config:/app/config
      - ./tools:/app/tools
      - ./clients:/app/clients
      - ./secrets.local.json:/app/secrets.local.json:ro
      - ~/.eislaw/store:/root/.eislaw/store
    depends_on:
      - meili
    restart: unless-stopped

  web:
    image: node:20-alpine
    working_dir: /app
    volumes:
      - ./frontend:/app
    environment:
      - VITE_API_URL=http://20.217.86.4:8799
      - CHOKIDAR_USEPOLLING=true
    command: /bin/sh -c "npm install && npm run dev -- --host --port 5173"
    ports:
      - "${WEB_PORT:-5173}:5173"
    depends_on:
      - api
    restart: unless-stopped

  meili:
    image: getmeili/meilisearch:latest
    environment:
      - MEILI_NO_ANALYTICS=true
    command: ["/bin/meilisearch", "--env", "development", "--http-addr", "0.0.0.0:7700"]
    ports:
      - "${MEILI_PORT:-7700}:7700"
    volumes:
      - meili_data:/meili_data
    restart: unless-stopped

volumes:
  meili_data:
