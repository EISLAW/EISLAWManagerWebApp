Place exported `.fig` files here so they can be tracked in Git.

Recommended naming: `<feature>-<version>.fig` (e.g., `task-owner-workflow_v1.fig`).

Add a short note at the top of this file with the latest exports so teammates know which files exist.
