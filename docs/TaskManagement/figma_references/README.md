Use this folder for any image or PDF references that Figma’s AI should consider alongside the `.fig` file.

Recommended approach:
1. Export PNG/JPG snapshots of relevant frames (Dashboard, Client Tasks, Task Modal).
2. Name files descriptively, e.g., `dashboard_tasks_rtl.png`, `task_modal_owner_chip_before.png`.
3. Mention these paths explicitly in prompts, for example:
   ```
   See docs/TaskManagement/figma_references/task_modal_owner_chip_before.png
   ```

Once you drop the reference images here I’ll incorporate them into the prompt automatically.
