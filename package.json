{
  "name": "eislaw-system",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "ui:test": "playwright test",
    "ui:install": "playwright install --with-deps",
    "ui:report": "playwright show-report",
    "puppeteer:probe": "node tools/puppeteer_probe.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.46.0",
    "puppeteer": "^24.29.0"
  }
}
